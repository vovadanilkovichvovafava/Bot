[phases.setup]
nixPkgs = ["nodejs_18"]

# npm ci requires a committed package-lock.json (none here) — use npm install
[phases.install]
cmds = ["npm install --omit=dev"]

[start]
cmd = "node index.js"
