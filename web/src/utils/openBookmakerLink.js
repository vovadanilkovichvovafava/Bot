/**
 * Opens bookmaker link — simple version, just regular link behavior.
 */
export function openBookmakerLink(url, event) {
  if (!url) return;
  // Let the default <a target="_blank"> behavior work
  return;
}
